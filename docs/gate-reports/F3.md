# Gate Report — F3 Pagamenti & Ricevute PDF

**Data**: 2026-06-05  
**Verdetto**: PASSATO (quarto round)  
**npm run verify**: Verde (200 test pass, 1 skip, typecheck/lint/build OK)

## Verifiche DoD

| Criterio | Esito |
|---|---|
| Stato pagamento (pagato/da_incassare) e metodo (contanti/POS/bonifico) | PASS |
| Numerazione progressiva per anno della data di emissione | PASS |
| Numero iniziale configurabile dall'utente (invariante 6) | PASS — SettingsPage + UPSERT su app_settings SQLite |
| Assegnazione al salvataggio, immutabilità sui re-download | PASS |
| Due copie per pagina (copia cliente + copia matrice) | PASS |
| Ricevuta multi-riga (voci pagabili + riga libera) | PASS |
| Annullamento mantiene numero, voci tornano da_incassare (invariante 5) | PASS |
| Template con logo e dati attività | PASS |
| Importi € IVA inclusa, date gg/mm/aaaa | PASS |

## Gate test soddisfatti

- Unit numerazione: reset annuale, numero iniziale (100), immutabilità → 5 test
- E2E-equivalent "transazione → ricevuta salvata → re-download stesso numero" → 2 test
- Invariante 5 annullamento → 6 test
- Unit formatImporto / formatDataIT → 8 test

## Fix applicati lungo i round

1. Primo round: preload mancava `pdf.genera` (B1); indirizzo tutore null nel template (B2)
2. Secondo round: dicitura_pie non in AppSettings (DC1); tipi ElectronAPI divergenti (DC2)
3. Terzo round: test E2E-equivalent aggiunto; SettingsPage creata per `receipt_start_number`
4. Quarto round: UPSERT SQLite da `settings:set` per sincronizzare `receipt_start_number` e `dicitura_pie`

## File chiave

- `src/main/db/migrations/004_receipts.ts` — schema ricevute + righe
- `src/main/db/receipts-repository.ts` — creaRicevuta (atomica), annullaRicevuta, numerazione
- `src/main/domain/ricevuta.ts` — template HTML (due copie, tutore, indirizzo)
- `src/main/domain/ricevuta-format.ts` — formatImporto, formatDataIT
- `src/main/pdf/generator.ts` — generaPDFInElectron (BrowserWindow nascosta)
- `src/renderer/src/pages/SettingsPage.tsx` — receipt_start_number configurabile
- `src/renderer/src/components/receipts/EmittiRicevutaForm.tsx`
- `src/renderer/src/pages/ReceiptsPage.tsx`
- `tests/unit/receipts-invariants.test.ts` — 26 test (24 originali + 2 E2E-equivalent)
